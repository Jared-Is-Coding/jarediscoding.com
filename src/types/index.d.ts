import { ReactElement } from "react"

type projectData = {
    text: string[]
    image: string
    imageTitle: string
    imageAlt: string
    url: string[]
    target: string
    position?: string
    technologies?: ReactElement
}